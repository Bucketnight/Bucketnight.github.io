NAME=ncurses
VERSION=6.4
SRC=https://ftp.gnu.org/gnu/ncurses/ncurses-6.4.tar.gz
DEPENDENCIES=

build() {
    ./configure --prefix=/usr --with-shared --enable-widec
    make
}

install() {
    make install
}